@model IBP2.StudentResultManagementSystemMVC.Models.CreateResultRequest

@{
    ViewData["Title"] = "Record Result";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card border-0 shadow-lg rounded-4">
                <div class="card-header bg-indigo text-white p-4 rounded-top-4">
                    <h2 class="h4 mb-0 fw-bold">Record Examination Result</h2>
                </div>
                <div class="card-body p-4 p-md-5">
                    <form asp-action="Create">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-4"></div>

                        <div class="mb-4">
                            <label asp-for="StudentId" class="form-label fw-semibold text-secondary">Select Student</label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-end-0">
                                    <i class="bi bi-person text-indigo"></i>
                                </span>
                                <select asp-for="StudentId" class="form-select bg-light border-start-0 ps-0" asp-items="ViewBag.Students">
                                    <option value="">-- Choose Student --</option>
                                </select>
                            </div>
                            <span asp-validation-for="StudentId" class="text-danger small mt-1 d-block"></span>
                        </div>

                        <div class="mb-4">
                            <label asp-for="SubjectId" class="form-label fw-semibold text-secondary">Select Subject</label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-end-0">
                                    <i class="bi bi-book text-indigo"></i>
                                </span>
                                <select asp-for="SubjectId" class="form-select bg-light border-start-0 ps-0" asp-items="ViewBag.Subjects">
                                    <option value="">-- Choose Subject --</option>
                                </select>
                            </div>
                            <span asp-validation-for="SubjectId" class="text-danger small mt-1 d-block"></span>
                        </div>

                        <div class="mb-5">
                            <label asp-for="Marks" class="form-label fw-semibold text-secondary">Obtained Marks</label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-end-0">
                                    <i class="bi bi-check2-circle text-indigo"></i>
                                </span>
                                <input asp-for="Marks" type="number" class="form-control bg-light border-start-0 ps-0" placeholder="Enter marks (0-100)" min="0" max="100" />
                            </div>
                            <span asp-validation-for="Marks" class="text-danger small mt-1 d-block"></span>
                        </div>

                        <div class="d-grid gap-3">
                            <button type="submit" class="btn btn-indigo btn-lg shadow-sm py-3 fw-bold text-white">
                                Save Result
                            </button>
                            <a asp-action="Index" class="btn btn-outline-secondary py-2">
                                Back to List
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <style>
        .bg-indigo { background-color: #6610f2; }
        .btn-indigo { background-color: #6610f2; border-color: #6610f2; }
        .btn-indigo:hover { background-color: #520dc2; border-color: #520dc2; }
        .form-select:focus, .form-control:focus {
            background-color: #fff !important;
            border-color: #6610f2;
            box-shadow: 0 0 0 0.25rem rgba(102, 16, 242, 0.15);
        }
        .input-group-text { color: #6c757d; }
    </style>
}
